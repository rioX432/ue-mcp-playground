# Unreal Engine generated/build artifacts — never commit these
Binaries/
DerivedDataCache/
Intermediate/
Saved/
.vs/
.vscode/
*.VC.db
*.opensdf
*.sdf
*.sln
*.suo
*.xcodeproj/
*.xcworkspace/

# OS / tooling
.DS_Store
Thumbs.db

# Python bytecode (editor/MCP Python toolsets)
__pycache__/
*.pyc

# Custom-tool screenshot outputs (timestamped, regenerable); commit curated ones explicitly
docs/experiments/screenshots/custom-*.png

# Editor-lane lock (runtime, machine-local)
.claude/.editor-lane.lock/

# Keep these tracked:
#   Source/  Config/  Content/  *.uproject  .claude/  docs/  Plugins/<your plugins>

# Imported Fab / template content — large, re-acquirable from Fab Library or
# "Add Feature or Content Pack". Not committed (GitHub LFS free quota is ~1GB;
# the revolver pack alone is ~971MB). BP_Hero/BP_Enemy reference these by path;
# re-add them at the same /Game paths to restore the references.
/Content/HS_WeaponPack_03/
/Content/Characters/
/Content/ThirdPerson/
/Content/Sci-Fi_Shots_Pack2_Game_Of_Weapons/
/Content/alstra_infinite/
/Content/Fab/
/Content/LevelPrototyping/
/Content/Input/
/Content/__ExternalActors__/
/Content/__ExternalObjects__/
